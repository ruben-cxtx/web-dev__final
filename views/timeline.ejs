<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Timeline</title>
    <link href="/styles/styles.css" rel="stylesheet">
    <link href="/styles/timeline.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/8f4601af06.js" crossorigin="anonymous"></script>
</head>
<body>
<%- include("partials/header") %>
<main>
    <section class="hero-timeline" aria-label="hero">
        <div class="hero-timeline__text">
            <h1 class="title">Timeline</h1>
            <p class="text">An overview of Panama’s internet journey from dial-up to 5G. Explore major milestones, broadband expansion, and government policy shaping connectivity from 2001 to 2025.</p>
        </div>
        <img src="/assets/timeline-hero.jpg" alt="Clock Image">
    </section>

</main>
<section class="key-insights" aria-label="key insights">
    <h2 class="subtitle">Key Insights</h2>
    <div class="cards__container">
        <%let j = 0;%>
    <% keyInsights.forEach(element => { %>
        <div class="ins__card" aria-label="insight card">
            <h4 class="x-small-title"><%= element.subtitle %></h4>
            <h3 class="small-title highlight-<%=colors[j]%>"><%= element.title %></h3>
            <p class="text"><%= element.text %></p>
        </div>
            <% j++;%>
    <% }); %>
    </div>
</section>

<section class="timeline__container" aria-label="timeline">
    <h2 class="subtitle">Timeline</h2>
    <div class="cards__container">
        <%let i = 0;%>
    <% timeline.forEach(element => { %>
        <div class="time__card" aria-label="insight card">

            <div class="time__year" id="<%= colors[i]%>">
                <h2><%= element.year %></h2>
            </div>
            <div class="time__text">
                <h3 class="small-title"><%= element.title %></h3>
                <p class="text"><%= element.text %></p>
            </div>
            <% i++;%>
        </div>
    <% }); %>
    </div>
</section>
<%- include("partials/footer") %>
</body>
</html>
